# Markdown mirrors

Plain-text mirrors of the public archive pages. The HTML files at the repository root are canonical for the public site.

Keep these files in sync when the matching `.html` page changes. Do not invent content.
